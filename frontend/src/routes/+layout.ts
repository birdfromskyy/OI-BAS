/**
 * Приложение работает как одностраничное: разметку собирает устройство.
 *
 * Это требование офлайна, а не вкусовщина. При серверном рендере каждая
 * страница приходит со своей разметкой, и когда связи нет, service worker
 * может отдать только ту, что успела попасть в кэш, — чужая разметка ломает
 * гидратацию и даёт ошибку 500. Без серверного рендера все маршруты делят
 * одну пустую оболочку: она кэшируется один раз и годится любому экрану,
 * даже тому, который на этом устройстве ещё не открывали.
 *
 * Данные всё равно лежат на устройстве (localStorage), поэтому серверный
 * рендер ничего полезного не добавлял: он рисовал моки, которые тут же
 * заменялись сохранёнными записями.
 */
export const ssr = false;
